# Copy to .env.local when implementation starts.
# Never commit real secret values.

OPENWEATHER_API_KEY=
WEATHER_PROVIDER=openweathermap
DEFAULT_CITY=Jakarta
DEFAULT_LOCALE=id
DEFAULT_UNITS=metric
APP_BASE_URL=http://localhost:3000

# Optional; do not enable before privacy/redaction review.
SENTRY_DSN=
